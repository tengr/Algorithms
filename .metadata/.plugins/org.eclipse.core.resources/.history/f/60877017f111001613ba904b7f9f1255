package AnonymousClass;

public class Map<I,O>{
	O[] map(Function func,I[] inp){
		for(int i=0;i<inp.length;i++){
			inp[i]  = (I) func.func(inp[i]);
		}
		return (O[])inp;
	}
	int[] map2(Function func,int[] inp){
		for(int i=0;i<inp.length;i++){
			inp[i]  = func2.func(inp[i]);
		}
		return inp;
	}


}

interface Function<I,O>{
	O func(I input);
	int func2(int input);
}
