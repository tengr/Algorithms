import java.util.concurrent.*;
public class CuncurrentCollections {
	public traverse(){
		Map<Integer, Integer> map = CuncurrentHashMap<Integer, Integer>();
		for(int i = 0; i < 10; i++) map.put(i,i);
	}
}
